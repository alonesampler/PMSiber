# PMS — Project Management System

Тестовый проект системы управления **проектами и сотрудниками**.

Проект реализован как pet / test assignment с упором на **архитектуру**, **читаемость кода** и **базовые DDD-принципы**, а не на UI-вылизанность.

---

## 📌 Описание

PMS — это простая система для:
- управления проектами
- назначения менеджеров и исполнителей
- загрузки и хранения документов проектов
- поиска сотрудников

Проект разделён на **Backend** и **Frontend**, запускается через **Docker Compose**.

---

## 🧠 Архитектура Backend

Backend реализован на **ASP.NET Core** с использованием:

- **Clean Architecture**
  - Domain
  - Application
  - Infrastructure
  - API
- частичного **DDD-подхода**
  - Entities
  - Value Objects
  - Aggregate Root (`Project`)
- Result Pattern (`FluentResults`)
- Repository + Unit of Work
- EF Core + MS SQL Server

Основные доменные сущности:
- `Project`
- `Employee`
- `Document`

Проект (`Project`) является **Aggregate Root** и управляет:
- менеджером
- исполнителями
- связанными документами

---

## 🎨 Frontend

Frontend реализован на:
- **React**
- **Vite**
- **TypeScript**
- обычный CSS (без UI-фреймворков)

Фокус сделан на:
- простую структуру
- понятную навигацию
- минимализм без лишней сложности

---

## 🐳 Docker & Infrastructure

Проект запускается через `docker-compose` и включает:

- **backend** — ASP.NET Core API
- **proj_mssql** — MS SQL Server 2022
- **frontend_react** — Vite + React

---

## ▶️ Запуск проекта

### 1. Требования
- Docker
- Docker Compose

### 2. Запуск

Из корня проекта:

```bash
docker compose up --build

(каюсь так как в фронтенде я не силён мои соавторами были 2-ИИ, StackOverflow, Youtube)